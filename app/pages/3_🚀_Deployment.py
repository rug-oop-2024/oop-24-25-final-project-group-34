import streamlit as st

st.set_page_config(page_title="Deployment", page_icon="🚀")

st.write("""
         ### When one cannot save a pipeline they can definitly
         ### not display one either, but it would be just sad to
         ### not use this page now, so this is it😀
         """)
